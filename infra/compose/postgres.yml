services:
  postgres:
    image: postgres:16-alpine
    ports: ["5432:5432"]
    environment:
      POSTGRES_USER: veripay
      POSTGRES_PASSWORD: veripay
      POSTGRES_DB: customer_db
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
