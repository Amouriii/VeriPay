[TEMPLATE]
VITE_API_BASE=http://localhost:8000
# Analyst console: leave empty to use the MSW mocks; point this at the
# analyst_api service (default local port) to serve the console live.
# VITE_ANALYST_API_BASE=http://localhost:8026