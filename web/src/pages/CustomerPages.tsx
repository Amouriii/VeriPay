import { useMemo, useState, type ReactNode } from 'react';
import { Link, useParams } from 'react-router-dom';
import { activityEvents, customerAccounts, customerDevices, customerNotifications, customerTransactions, type CustomerTransaction } from '../customerData';

const money = (amount: number) => new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(Math.abs(amount));
const statusStyle = (status: string) => status === 'Completed' ? 'bg-emerald-50 text-emerald-700' : status === 'Verification Required' || status === 'Pending' ? 'bg-amber-50 text-amber-700' : status === 'Blocked' || status === 'Denied' || status === 'Failed' ? 'bg-red-50 text-red-700' : 'bg-slate-100 text-slate-600';
function Badge({ children }: { children: string }) { return <span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${statusStyle(children)}`}>{children}</span>; }
function Panel({ children, className = '' }: { children: ReactNode; className?: string }) { return <section className={`rounded-2xl border border-slate-200 bg-white shadow-sm ${className}`}>{children}</section>; }
function Heading({ label, title, detail, action }: { label: string; title: string; detail: string; action?: ReactNode }) { return <div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal-700">{label}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-2 text-sm text-slate-500">{detail}</p></div>{action}</div>; }
function AccountCard({ account }: { account: typeof customerAccounts[number] }) { return <Panel className="relative overflow-hidden p-6"><div className="absolute right-0 top-0 h-24 w-24 rounded-bl-full bg-teal-50" /><p className="relative text-sm font-semibold text-slate-500">{account.name}</p><p className="relative mt-2 text-sm tracking-[0.22em] text-slate-600">•••• {account.last4}</p><p className="mt-7 text-xs uppercase tracking-[0.14em] text-slate-400">Available balance</p><p className="mt-1 text-3xl font-semibold tracking-tight">{money(account.balance)}</p><p className="mt-3 text-xs text-slate-500">{account.type} · <span className="text-emerald-700">{account.status}</span></p></Panel>; }
function SecurityCard() { return <Panel className="border-teal-200 bg-[#effaf8] p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.15em] text-teal-700">Your security</p><h2 className="mt-2 text-xl font-semibold">Your account is protected</h2></div><span className="grid h-10 w-10 place-items-center rounded-full bg-white text-xl text-emerald-600">✓</span></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><p className="text-xs text-slate-500">Two-factor authentication</p><p className="mt-1 font-semibold">Enabled</p></div><div><p className="text-xs text-slate-500">Trusted devices</p><p className="mt-1 font-semibold">2 devices</p></div><div><p className="text-xs text-slate-500">Recent security alerts</p><p className="mt-1 font-semibold">1 to review</p></div><div><p className="text-xs text-slate-500">Last login</p><p className="mt-1 font-semibold">Today, 3:42 PM</p></div></div><Link to="/customer/security" className="mt-6 inline-block text-sm font-bold text-teal-800">View Security Center -&gt;</Link></Panel>; }
function TransactionList({ items, compact = false }: { items: CustomerTransaction[]; compact?: boolean }) { return <div className="divide-y divide-slate-100">{items.map((transaction) => <Link key={transaction.id} to={`/customer/transactions/${transaction.id}`} className="flex items-center justify-between gap-4 px-5 py-4 hover:bg-slate-50"><div className="flex min-w-0 items-center gap-3"><div className={`grid h-10 w-10 shrink-0 place-items-center rounded-full text-sm font-bold ${transaction.status === 'Verification Required' ? 'bg-amber-100 text-amber-800' : 'bg-slate-100 text-slate-600'}`}>{transaction.merchant.slice(0, 1)}</div><div className="min-w-0"><p className="truncate font-semibold">{transaction.merchant}</p><p className="mt-1 text-xs text-slate-500">{transaction.date} · {transaction.type}</p></div></div><div className="text-right"><p className={`font-semibold ${transaction.amount > 0 ? 'text-emerald-700' : ''}`}>{transaction.amount > 0 ? '+' : '-'}{money(transaction.amount)}</p>{!compact && <div className="mt-1"><Badge>{transaction.status}</Badge></div>}</div></Link>)}</div>; }

export function CustomerDashboard() { const flagged = customerTransactions.filter((item) => item.status === 'Verification Required'); return <div className="mx-auto max-w-6xl px-6 py-8"><Heading label="Personal overview" title="Good morning, Jordan" detail="Here&apos;s an overview of your accounts and security." /><div className="grid gap-5 md:grid-cols-2">{customerAccounts.map((account) => <AccountCard key={account.last4} account={account} />)}</div>{flagged.map((transaction) => <Panel key={transaction.id} className="mt-5 border-amber-200 bg-amber-50 p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2 text-sm font-bold text-amber-800"><span>!</span> Verification required</div><h2 className="mt-2 text-lg font-semibold">{transaction.merchant} · {money(transaction.amount)}</h2><p className="mt-1 text-sm text-amber-900">This purchase looks different from your usual activity.</p></div><Link to={`/customer/transactions/${transaction.id}`} className="rounded-md bg-[#102a2a] px-4 py-2.5 text-center text-sm font-semibold text-white">Review transaction</Link></div></Panel>)}<div className="mt-6 grid gap-6 lg:grid-cols-[1.25fr_1fr]"><Panel><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 className="font-semibold">Recent transactions</h2><p className="mt-1 text-xs text-slate-500">Your latest account activity</p></div><Link to="/customer/transactions" className="text-sm font-bold text-teal-700">View all</Link></div><TransactionList items={customerTransactions.slice(1, 5)} /></Panel><SecurityCard /></div><Panel className="mt-6 overflow-hidden"><div className="border-b border-slate-200 px-5 py-4"><h2 className="font-semibold">Your usual activity</h2><p className="mt-1 text-xs text-slate-500">A simple view of patterns that help protect your account</p></div><div className="grid gap-5 p-5 sm:grid-cols-2 lg:grid-cols-4"><Behavior label="Average purchase" value="$42.50" /><Behavior label="Typical range" value="$5 - $120" /><Behavior label="Normal location" value="New York · 82%" /><Behavior label="Typical time" value="12 PM - 8 PM" /></div><div className="grid gap-4 border-t border-slate-200 p-5 md:grid-cols-2"><ActivityBars title="Normal locations" items={[['New York', 82], ['New Jersey', 12], ['Other', 6]]} /><ActivityBars title="Typical devices" items={[['iPhone 15 Pro', 72], ['MacBook Pro', 18], ['Other', 10]]} /></div><Link to="/customer/profile" className="block border-t border-slate-200 px-5 py-4 text-sm font-bold text-teal-700">View your Normal Activity profile -&gt;</Link></Panel></div>; }
function Behavior({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-slate-500">{label}</p><p className="mt-2 font-semibold">{value}</p></div>; }
function ActivityBars({ title, items }: { title: string; items: [string, number][] }) { return <div><p className="text-xs font-bold uppercase tracking-[0.12em] text-slate-400">{title}</p><div className="mt-3 space-y-3">{items.map(([label, percent]) => <div key={label}><div className="mb-1 flex justify-between text-xs"><span>{label}</span><strong>{percent}%</strong></div><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-teal-600" style={{ width: `${percent}%` }} /></div></div>)}</div></div>; }

export function CustomerAccountsPage() { return <div className="mx-auto max-w-6xl px-6 py-8"><Heading label="Money" title="Accounts" detail="Your balances and account details." /><div className="grid gap-5 md:grid-cols-2">{customerAccounts.map((account) => <AccountCard key={account.last4} account={account} />)}</div><Panel className="mt-6 p-6"><h2 className="font-semibold">Account security</h2><p className="mt-2 text-sm leading-6 text-slate-500">Your account numbers and payment details are protected. We only show the last four digits in the VeriPay experience.</p></Panel></div>; }

export function CustomerTransactionsPage() { const [query, setQuery] = useState(''); const [status, setStatus] = useState('All'); const list = useMemo(() => customerTransactions.filter((item) => `${item.id} ${item.merchant}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All' || item.status === status)), [query, status]); return <div className="mx-auto max-w-6xl px-6 py-8"><Heading label="Activity" title="Transactions" detail="Review your payment history and security status." /><Panel className="overflow-hidden"><div className="flex flex-col gap-3 border-b border-slate-200 p-5 sm:flex-row"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search transactions..." className="flex-1 rounded-md border border-slate-300 px-3 py-2.5 text-sm" /><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-md border border-slate-300 px-3 py-2.5 text-sm"><option>All</option><option>Completed</option><option>Pending</option><option>Verification Required</option><option>Blocked</option></select></div><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-[0.1em] text-slate-500"><tr>{['Merchant', 'Amount', 'Date', 'Type', 'Status', 'Security'].map((heading) => <th key={heading} className="px-5 py-3">{heading}</th>)}</tr></thead><tbody>{list.map((item) => <tr key={item.id} className="border-t border-slate-100 hover:bg-slate-50"><td className="px-5 py-4"><Link to={`/customer/transactions/${item.id}`} className="font-semibold text-teal-800">{item.merchant}</Link><p className="text-xs text-slate-500">{item.id}</p></td><td className="px-5 py-4 font-semibold">{item.amount > 0 ? '+' : '-'}{money(item.amount)}</td><td className="px-5 py-4 text-slate-500">{item.date}</td><td className="px-5 py-4">{item.type}</td><td className="px-5 py-4"><Badge>{item.status}</Badge></td><td className="px-5 py-4 text-xs text-slate-500">{item.securityNote ?? 'Looks normal'}</td></tr>)}</tbody></table></div><div className="md:hidden"><TransactionList items={list} /></div>{list.length === 0 && <p className="px-5 py-10 text-center text-sm text-slate-500">No transactions match your search.</p>}</Panel></div>; }

export function CustomerTransactionDetail() { const { id } = useParams(); const transaction = customerTransactions.find((item) => item.id === id) ?? customerTransactions[0]; const [verification, setVerification] = useState<'idle' | 'waiting' | 'approved' | 'denied'>(transaction.status === 'Verification Required' ? 'idle' : 'approved'); const unusual = transaction.status === 'Verification Required' || transaction.status === 'Blocked'; return <div className="mx-auto max-w-5xl px-6 py-8"><Link to="/customer/transactions" className="text-sm font-semibold text-teal-700">&lt;- Back to transactions</Link><div className="mt-6 flex flex-col justify-between gap-4 md:flex-row md:items-start"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal-700">Transaction details</p><h1 className="mt-2 text-3xl font-semibold">{transaction.merchant}</h1><p className="mt-2 text-sm text-slate-500">Transaction #{transaction.id}</p></div><Badge>{verification === 'approved' ? 'Completed' : verification === 'denied' ? 'Denied' : transaction.status}</Badge></div><div className="mt-6 grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><Panel className="p-6"><p className="text-sm text-slate-500">Amount</p><p className="mt-2 text-4xl font-semibold tracking-tight">{transaction.amount > 0 ? '+' : '-'}{money(transaction.amount)}</p><div className="mt-8 grid gap-5 sm:grid-cols-2">{[['Merchant', transaction.merchant], ['Currency', transaction.currency], ['Date and time', transaction.date], ['Transaction type', transaction.type], ['Payment channel', transaction.channel], ['Location', transaction.location]].map(([label, value]) => <div key={label}><p className="text-xs text-slate-500">{label}</p><p className="mt-1 font-medium">{value}</p></div>)}</div></Panel>{unusual ? <Panel className="border-amber-200 bg-amber-50 p-6"><p className="text-xs font-bold uppercase tracking-[0.15em] text-amber-800">Security check</p><h2 className="mt-2 text-xl font-semibold">Why are we asking you to verify?</h2><p className="mt-3 text-sm leading-6 text-amber-950">This transaction is different from your usual activity. It may be because it is a larger purchase, from a new device, or from an unusual location.</p>{verification === 'idle' && <button onClick={() => setVerification('waiting')} className="mt-5 w-full rounded-md bg-[#102a2a] px-4 py-3 text-sm font-semibold text-white">Review verification</button>}{verification === 'waiting' && <div className="mt-5 rounded-md bg-white p-4 text-sm text-slate-700"><p className="font-semibold">Check your VeriPay mobile app</p><p className="mt-1">Approve or deny this transaction securely from your phone.</p><div className="mt-4 flex gap-2"><button onClick={() => setVerification('approved')} className="rounded-md bg-emerald-600 px-3 py-2 text-xs font-bold text-white">Approve</button><button onClick={() => setVerification('denied')} className="rounded-md border border-red-200 px-3 py-2 text-xs font-bold text-red-700">Deny</button></div></div>}{verification === 'approved' && <p className="mt-5 rounded-md bg-emerald-100 p-3 text-sm font-semibold text-emerald-800">✓ Transaction approved</p>}{verification === 'denied' && <p className="mt-5 rounded-md bg-red-100 p-3 text-sm font-semibold text-red-800">✕ Transaction denied</p>}</Panel> : <Panel className="p-6"><p className="text-xs font-bold uppercase tracking-[0.15em] text-teal-700">Security information</p><h2 className="mt-2 text-xl font-semibold">This transaction looks normal</h2><p className="mt-3 text-sm leading-6 text-slate-500">It matches your recent activity and no action is needed.</p></Panel>}</div>{unusual && <Panel className="mt-5 p-6"><h2 className="text-lg font-semibold">What looks different?</h2><div className="mt-5 grid gap-4 md:grid-cols-3"><Reason title="Purchase amount" detail="Your usual purchases are $5 - $120." /><Reason title="Device or location" detail="This activity may come from somewhere new." /><Reason title="Transaction time" detail="The timing is different from your recent activity." /></div></Panel>}</div>; }
function Reason({ title, detail }: { title: string; detail: string }) { return <div className="rounded-xl bg-slate-50 p-4"><p className="font-semibold">{title}</p><p className="mt-2 text-sm leading-5 text-slate-500">{detail}</p></div>; }

export function SecurityPage() { return <div className="mx-auto max-w-6xl px-6 py-8"><Heading label="Protection" title="Security Center" detail="Manage the ways VeriPay helps protect your account." /><Panel className="border-emerald-200 bg-emerald-50 p-6"><div className="flex items-center gap-4"><span className="grid h-11 w-11 place-items-center rounded-full bg-white text-xl text-emerald-600">✓</span><div><h2 className="text-xl font-semibold">Your account is protected</h2><p className="mt-1 text-sm text-emerald-800">No urgent security action is required.</p></div></div></Panel><div className="mt-6 grid gap-5 md:grid-cols-2">{[['Two-factor authentication', 'Enabled', 'Extra protection is active for your sign-ins.'], ['Password', 'Last changed 30 days ago', 'A strong password helps keep your account safe.'], ['Trusted devices', '2 trusted devices', 'Review devices that can access your account.'], ['Transaction verification', 'Enabled', 'Unusual transactions may need your approval.']].map(([title, value, detail]) => <Panel key={title} className="p-5"><p className="font-semibold">{title}</p><p className="mt-2 text-emerald-700">{value}</p><p className="mt-2 text-sm text-slate-500">{detail}</p></Panel>)}</div><Panel className="mt-6 overflow-hidden"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><h2 className="font-semibold">Trusted devices</h2><Link to="/customer/security/devices" className="text-sm font-bold text-teal-700">Manage devices</Link></div><div className="divide-y divide-slate-100">{customerDevices.map((device) => <div key={device.name} className="flex items-center justify-between px-5 py-4"><div><p className="font-semibold">{device.name}</p><p className="mt-1 text-xs text-slate-500">{device.os} · Last active {device.lastActive}</p></div><Badge>{device.trusted ? 'Trusted' : 'New device'}</Badge></div>)}</div></Panel></div>; }

export function DevicesPage() { return <div className="mx-auto max-w-5xl px-6 py-8"><Heading label="Protection" title="Trusted Devices" detail="Only keep devices you recognize connected to your account." /><div className="space-y-4">{customerDevices.map((device) => <Panel key={device.name} className="flex flex-col justify-between gap-4 p-5 sm:flex-row sm:items-center"><div><div className="flex items-center gap-3"><h2 className="font-semibold">{device.name}</h2><Badge>{device.trusted ? 'Trusted' : 'New device'}</Badge></div><p className="mt-2 text-sm text-slate-500">{device.os} · First seen {device.firstSeen} · Last active {device.lastActive}</p></div><button className={`rounded-md px-3 py-2 text-xs font-bold ${device.trusted ? 'border border-slate-300 text-slate-600' : 'border border-red-200 text-red-700'}`}>{device.trusted ? 'Remove device' : "This isn't my device"}</button></Panel>)}</div></div>; }

export function ActivityPage() { return <div className="mx-auto max-w-5xl px-6 py-8"><Heading label="Protection" title="Security Activity" detail="A timeline of important activity on your account." /><Panel className="divide-y divide-slate-100">{activityEvents.map(([time, title, detail, tone]) => <div key={`${time}-${title}`} className="flex gap-4 p-5"><span className={`mt-1 grid h-8 w-8 shrink-0 place-items-center rounded-full ${tone === 'attention' ? 'bg-amber-100 text-amber-700' : 'bg-emerald-100 text-emerald-700'}`}>{tone === 'attention' ? '!' : '✓'}</span><div><p className="text-xs text-slate-500">{time}</p><p className="mt-1 font-semibold">{title}</p><p className="mt-1 text-sm text-slate-500">{detail}</p></div></div>)}</Panel></div>; }

export function NotificationsPage() { const [items, setItems] = useState(customerNotifications); const markAll = () => setItems((current) => current.map((item) => ({ ...item, unread: false }))); return <div className="mx-auto max-w-5xl px-6 py-8"><Heading label="Stay informed" title="Notifications" detail="Security updates and transaction alerts from VeriPay." action={<button onClick={markAll} className="rounded-md border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold">Mark all as read</button>} /><Panel className="divide-y divide-slate-100">{items.map((item, index) => <div key={item.title} className={`flex gap-4 p-5 ${item.unread ? 'bg-teal-50/40' : ''}`}><span className={`mt-1 grid h-9 w-9 shrink-0 place-items-center rounded-full ${item.severity === 'safe' ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-700'}`}>{item.severity === 'safe' ? '✓' : '!'}</span><div className="flex-1"><div className="flex justify-between gap-3"><h2 className="font-semibold">{item.title}</h2>{item.unread && <span className="text-xs font-bold text-teal-700">Unread</span>}</div><p className="mt-1 text-sm text-slate-600">{item.description}</p><p className="mt-2 text-xs text-slate-400">{item.time}</p></div>{item.unread && <button onClick={() => setItems((current) => current.map((entry, entryIndex) => entryIndex === index ? { ...entry, unread: false } : entry))} className="self-start text-xs font-bold text-teal-700">Mark read</button>}</div>)}</Panel></div>; }

export function ReportFraudPage() { const [submitted, setSubmitted] = useState(false); return <div className="mx-auto max-w-3xl px-6 py-8"><Heading label="Account safety" title="Report Fraud" detail="Tell us about activity you do not recognize. We will help secure your account." />{submitted ? <Panel className="border-emerald-200 bg-emerald-50 p-8 text-center"><span className="text-3xl text-emerald-600">✓</span><h2 className="mt-3 text-xl font-semibold">Your report has been submitted.</h2><p className="mt-2 text-sm text-emerald-800">Reference number: FR-12345</p><Link to="/customer" className="mt-6 inline-block font-bold text-teal-800">Return to dashboard</Link></Panel> : <Panel className="p-6"><label className="block text-sm font-semibold">What would you like to report?<select className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2.5 font-normal"><option>Unrecognized transaction</option><option>Stolen card</option><option>Suspicious account activity</option><option>Unknown device</option><option>Account compromise</option><option>Other</option></select></label><label className="mt-5 block text-sm font-semibold">Transaction<select className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2.5 font-normal"><option>TX-89231 - Amazon - $850.00</option><option>TX-89202 - Northstar Travel - $2,340.00</option></select></label><label className="mt-5 block text-sm font-semibold">What happened?<textarea className="mt-2 min-h-32 w-full rounded-md border border-slate-300 px-3 py-2.5 font-normal" placeholder="Add details that may help us investigate..." /></label><button onClick={() => setSubmitted(true)} className="mt-5 rounded-md bg-[#102a2a] px-5 py-3 text-sm font-semibold text-white">Submit report</button></Panel>}</div>; }

export function ProfilePage() { return <div className="mx-auto max-w-6xl px-6 py-8"><Heading label="Personal details" title="Profile" detail="Manage your information and understand your normal activity." /><Panel className="p-6"><h2 className="text-lg font-semibold">Personal information</h2><div className="mt-5 grid gap-5 sm:grid-cols-2">{[['First name', 'Jordan'], ['Last name', 'Lee'], ['Email', 'j•••••@example.com'], ['Phone number', '+1 (•••) •••-0198'], ['Customer ID', 'CUS-18492']].map(([label, value]) => <div key={label}><p className="text-xs text-slate-500">{label}</p><p className="mt-1 font-medium">{value}</p></div>)}</div><button className="mt-6 rounded-md border border-slate-300 px-4 py-2.5 text-sm font-semibold">Edit personal information</button></Panel><Panel className="mt-6 p-6"><h2 className="text-lg font-semibold">Normal Behavior Profile</h2><p className="mt-2 text-sm leading-6 text-slate-500">Your activity pattern is based on your recent transaction history. It helps VeriPay recognize when a transaction may need an extra check.</p><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><Behavior label="Normal spending" value="$20 - $120" /><Behavior label="Average transaction" value="$42.50" /><Behavior label="Typical frequency" value="3 - 5 per day" /><Behavior label="Typical time" value="12 PM - 8 PM" /></div><div className="mt-6 grid gap-5 border-t border-slate-200 pt-6 md:grid-cols-3"><ActivityBars title="Normal locations" items={[['New York', 82], ['New Jersey', 12], ['Other', 6]]} /><ActivityBars title="Typical devices" items={[['iPhone 15 Pro', 72], ['MacBook Pro', 18], ['Other', 10]]} /><ActivityBars title="Common categories" items={[['Groceries', 32], ['Restaurants', 21], ['Shopping', 18]]} /></div><div className="mt-6 rounded-md bg-slate-50 p-4 text-sm text-slate-600"><strong className="text-slate-800">Typical activity time:</strong> 12 PM - 8 PM. Your normal activity may change over time as your spending patterns change.</div></Panel></div>; }

export function HelpPage() { return <div className="mx-auto max-w-5xl px-6 py-8"><Heading label="Support" title="Help & Support" detail="Find answers or reach the VeriPay support team." /><div className="grid gap-5 md:grid-cols-2">{[['Help Center', 'Find quick answers about accounts, payments, and verification.'], ['FAQ', 'Browse common questions about using VeriPay safely.'], ['Security Help', 'Learn what to do if you see an unfamiliar device or login.'], ['Fraud Help', 'Get guidance for reporting a transaction or securing your account.'], ['Contact Support', 'Our support team is available to help with account questions.']].map(([title, detail]) => <Panel key={title} className="p-6"><h2 className="font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{detail}</p><button className="mt-5 text-sm font-bold text-teal-700">Open {title} -&gt;</button></Panel>)}</div></div>; }
