// LLM investigation view (PLAN §20). Stubbed.
export function Investigation() {
  return (
    <div className="p-6 md:p-8">
      <p className="text-xs font-semibold uppercase tracking-[0.18em] text-accent">Investigation</p>
      <h1 className="mt-2 text-2xl font-bold tracking-tight text-ink">Investigation</h1>
      <div className="glass-panel mt-6 max-w-2xl rounded-2xl p-5">
        <p className="text-sm text-ink-muted">LLM-assisted investigation workspace is not wired yet.</p>
      </div>
    </div>
  );
}
