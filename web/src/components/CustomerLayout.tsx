import { useState, type ReactNode } from 'react';
import { NavLink } from 'react-router-dom';
import { customerNotifications } from '../customerData';

const links = [
  ['Dashboard', '/customer'], ['Accounts', '/customer/accounts'], ['Transactions', '/customer/transactions'],
  ['Security', '/customer/security'], ['Notifications', '/customer/notifications'], ['Report Fraud', '/customer/report-fraud'],
  ['Profile', '/customer/profile'], ['Help & Support', '/customer/help'],
];

export function CustomerLayout({ children }: { children: ReactNode }) {
  const [open, setOpen] = useState(false);
  const unread = customerNotifications.filter((notification) => notification.unread).length;
  return <div className="min-h-screen bg-[#f5f8f8] text-slate-900"><aside className={`fixed inset-y-0 left-0 z-30 flex w-64 flex-col border-r border-slate-200 bg-white transition-transform lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex h-20 items-center justify-between border-b border-slate-200 px-6"><div><p className="text-xl font-semibold tracking-tight text-[#102a2a]">VeriPay</p><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-teal-700">Secure banking</p></div><button className="text-slate-500 lg:hidden" onClick={() => setOpen(false)} aria-label="Close navigation">X</button></div><nav className="flex-1 space-y-1 px-3 py-5">{links.map(([label, href]) => <NavLink key={href} to={href} onClick={() => setOpen(false)} end={href === '/customer'} className={({ isActive }) => `block rounded-md px-3 py-2.5 text-sm font-medium ${isActive ? 'bg-teal-50 text-teal-800' : 'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}>{label}{label === 'Notifications' && unread > 0 && <span className="float-right rounded-full bg-red-100 px-2 py-0.5 text-[10px] font-bold text-red-700">{unread}</span>}</NavLink>)}</nav><div className="border-t border-slate-200 p-4"><div className="mb-4 flex items-center gap-2 text-xs text-emerald-700"><span className="h-2 w-2 rounded-full bg-emerald-500" />Account protected</div><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#102a2a] font-bold text-teal-200">JL</div><div><p className="text-sm font-semibold">Jordan Lee</p><p className="text-xs text-slate-500">Personal account</p></div></div><NavLink to="/customer/settings" className="mt-4 block text-xs text-slate-500 hover:text-slate-900">Settings</NavLink><button className="mt-3 w-full rounded-md border border-slate-200 py-2 text-left text-xs text-slate-500 hover:border-slate-400">Log out</button></div></aside>{open && <button className="fixed inset-0 z-20 bg-slate-950/30 lg:hidden" onClick={() => setOpen(false)} aria-label="Close navigation overlay" />}<div className="lg:pl-64"><header className="sticky top-0 z-10 flex h-20 items-center justify-between border-b border-slate-200 bg-[#f5f8f8]/95 px-5 backdrop-blur md:px-8"><div className="flex items-center gap-4"><button className="rounded-md border border-slate-300 bg-white px-3 py-2 text-sm lg:hidden" onClick={() => setOpen(true)}>Menu</button><span className="hidden text-sm text-slate-500 md:block">Personal banking</span></div><div className="flex items-center gap-4"><NavLink to="/customer/notifications" className="relative text-sm text-slate-600 hover:text-slate-900">Alerts{unread > 0 && <span className="absolute -right-2 -top-2 grid h-4 min-w-4 place-items-center rounded-full bg-red-600 px-1 text-[10px] text-white">{unread}</span>}</NavLink><span className="hidden h-6 w-px bg-slate-300 md:block" /><div className="hidden text-right md:block"><p className="text-sm font-semibold">Jordan Lee</p><p className="text-xs text-slate-500">•••• 4521</p></div><div className="grid h-9 w-9 place-items-center rounded-full bg-[#102a2a] text-xs font-bold text-teal-200">JL</div></div></header>{children}</div></div>;
}
