# Financial Context

**Implements:** PLAN §17

Cash-flow / behavioral baseline evaluation.

See `docs/architecture.md` for how this service fits the end-to-end pipeline.

## Develop
```bash
pip install -e .
pytest
uvicorn app.main:app --reload
```
